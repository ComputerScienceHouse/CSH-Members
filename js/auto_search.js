Ext.onReady(function(){
    /*
    var store = new Ext.data.JsonStore({
        url: update_url,
        root: 'data',  // the root of the array you'll send down
        idProperty: 'uid',
        fields: ['uid','cn']
    });
    
    var combo = new Ext.form.ComboBox({
        fieldLabel: 'This is cool',
        name: 'user_id',
        store: store,
        valueField: 'uid',
        displayField:'cn',
        typeAhead: true,
        mode: 'remote',
        queryParam: 'query',  //contents of the field sent to server.
        hideTrigger: true,    //hide trigger so it doesn't look like a combobox.
        selectOnFocus:true,
        width: 250,
        renderTo: 'person_field'  //the id of the html element to render to.
    //Not necessary if this is in an Ext formPanel.
    });
    */
});

function search()
{

}